import styled from "styled-components";

const AuthFormBlock = styled.div``;

function AuthForm() {
  return (
    <AuthFormBlock>
      AuthForm
    </AuthFormBlock>
  );
}

export default AuthForm;
